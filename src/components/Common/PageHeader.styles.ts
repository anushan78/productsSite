
import { style } from 'typestyle'
import { px } from 'csx'

export const headerStyle = style({
  background: 'rgb(28, 90, 135)',
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'space-between',
  padding: px(24),
  height: 'auto'
})
